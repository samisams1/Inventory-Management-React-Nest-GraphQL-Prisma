

const SimpleBarReact = require('simplebar-react/dist/simplebar-react');
//import simplebarReact from 'simplebar-react';
 //import { styled } from '@mui/material/styles';

//export const Scrollbar = styled(SimpleBar)``;

// Then, you can use the default export of the module as needed
export const { Scrollbar } = simplebarReact;